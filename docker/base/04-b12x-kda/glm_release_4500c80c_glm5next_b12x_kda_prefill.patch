--- a/vllm/models/glm5next/nvidia/kda.py
+++ b/vllm/models/glm5next/nvidia/kda.py
@@ -2,6 +2,8 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 """GLM-5.3-Flash KDA layer with separate convolutions and a bounded safe gate."""
 
+from typing import Any
+
 import torch
 from torch import nn
 
@@ -36,7 +38,13 @@
 from vllm.platforms import current_platform
 from vllm.third_party.flash_linear_attention.ops.kda import FusedRMSNormGated
 from vllm.transformers_utils.configs.glm5_next import Glm5NextConfig
+from vllm.utils.b12x import (
+    B12xWarmupUnit,
+    get_b12x_kda_prefill,
+    get_b12x_scratch_buffers,
+)
 from vllm.v1.attention.backends.gdn_attn import GDNAttentionMetadata
+from vllm.v1.attention.backends.utils import NULL_BLOCK_ID
 from vllm.v1.worker.workspace import current_workspace_manager
 
 if current_platform.is_rocm():
@@ -68,14 +76,45 @@
     )
 
 
+def _is_b12x_kda_prefill_supported(
+    head_dim: int,
+    dtype: torch.dtype,
+    lower_bound: float | None,
+    state_dtype: torch.dtype,
+) -> bool:
+    """Return whether the add-only B12X KDA prefill op supports this layer."""
+    api = get_b12x_kda_prefill()
+    if api is None or not current_platform.is_cuda():
+        return False
+    return (
+        head_dim == 128
+        and dtype == torch.bfloat16
+        and state_dtype == torch.float32
+        and lower_bound is not None
+        and api.is_supported(torch.device(current_platform.current_device()))
+    )
+
+
 def _resolve_kda_prefill_backend(
     backend: str,
     head_dim: int,
     dtype: torch.dtype,
     lower_bound: float | None,
+    state_dtype: torch.dtype = torch.float32,
 ) -> str:
-    if backend not in ("auto", "triton", "flashkda"):
+    if backend not in ("auto", "triton", "flashkda", "b12x"):
         raise ValueError(f"Unsupported KDA prefill backend: {backend}")
+    # Preserve A59's policy: auto remains Triton and B12X is explicit opt-in.
+    if backend == "b12x":
+        if not _is_b12x_kda_prefill_supported(
+            head_dim, dtype, lower_bound, state_dtype
+        ):
+            raise RuntimeError(
+                "The b12x KDA prefill backend requires the hash-pinned B12X "
+                "overlay on a supported CUDA device, bfloat16 activations, "
+                "head_dim=128, float32 recurrent state, and a bounded KDA gate."
+            )
+        return "b12x"
     # Keep GLM's existing auto policy; this candidate is explicit opt-in only.
     if backend != "flashkda":
         return "triton"
@@ -125,6 +164,82 @@
     return out, final_state
 
 
+class _B12xKdaPrefillWarmup:
+    """Compile the fixed-capacity KDA plan after the state pool is bound."""
+
+    def get_b12x_warmup_unit(
+        self,
+        layer: torch.nn.Module,
+        token_counts: tuple[int, ...],
+        output_dtype: torch.dtype,
+    ) -> B12xWarmupUnit:
+        del token_counts, output_dtype
+
+        def compile() -> None:
+            plan = layer._b12x_prefill_plan
+            api = layer._b12x_prefill_api
+            if plan is None or api is None:
+                return
+            scratch_buffers = get_b12x_scratch_buffers(plan)
+            scratch = (
+                scratch_buffers[0]
+                if len(scratch_buffers) == 1
+                else scratch_buffers
+            )
+            caps = plan.caps
+            device = caps.device
+            heads, head_dim = caps.heads, caps.head_dim
+            tokens = caps.chunk_tokens
+            rows = torch.zeros(
+                (tokens, heads, head_dim),
+                dtype=caps.model_dtype,
+                device=device,
+            )
+            indices = torch.zeros(1, dtype=torch.int32, device=device)
+            binding = api.bind(
+                plan,
+                scratch=scratch,
+                q=rows,
+                k=torch.zeros_like(rows),
+                v=torch.zeros_like(rows),
+                raw_g=torch.zeros_like(rows),
+                raw_beta=torch.zeros(
+                    (tokens, heads), dtype=caps.model_dtype, device=device
+                ),
+                A_log=layer.A_log.view(-1),
+                dt_bias=layer.dt_bias.view(-1, head_dim),
+                recurrent_state=layer.kv_cache[1],
+                cu_seqlens=torch.tensor(
+                    [0, tokens], dtype=torch.int32, device=device
+                ),
+                initial_state_indices=indices,
+                final_state_indices=indices,
+                checkpoint_state_indices=indices,
+                checkpoint_offsets=torch.zeros(
+                    1, dtype=torch.int32, device=device
+                ),
+                num_seqs=layer._b12x_prefill_num_seqs,
+                num_tokens=layer._b12x_prefill_num_tokens,
+                output=torch.zeros_like(rows),
+            )
+            api.prewarm(binding)
+
+        caps = getattr(layer._b12x_prefill_plan, "caps", None)
+        return B12xWarmupUnit(
+            name="KDA prefill",
+            key=(
+                type(layer),
+                None if caps is None else caps.device,
+                layer.local_num_heads,
+                layer.head_dim,
+                layer._b12x_prefill_max_tokens,
+                layer._b12x_prefill_max_seqs,
+                None if caps is None else caps.max_state_slots,
+            ),
+            compile=compile,
+        )
+
+
 class _Glm5NextMergedColumnParallelLinear(MergedColumnParallelLinear):
     """Merged projection with multiple replicated output shards.
 
@@ -364,6 +479,7 @@
             self.head_dim,
             vllm_config.model_config.dtype,
             self.kda_lower_bound,
+            self.get_state_dtype()[1],
         )
         self._flashkda_buffer_specs: (
             tuple[tuple[tuple[int, ...], torch.dtype], ...] | None
@@ -385,10 +501,163 @@
                 ((max_seqs, num_heads, head_dim, head_dim), self.get_state_dtype()[1]),
                 ((workspace_size,), torch.uint8),
             )
+        self._b12x_prefill_api: Any | None = None
+        self._b12x_prefill_plan = None
+        self._initialize_b12x_kda_prefill(vllm_config)
         # Process-global conv-state layout, resolved once here instead of on
         # every _forward call (it reads an env-derived flag each time).
         self._conv_state_dim_first = is_conv_state_dim_first()
 
+    def bind_kv_cache(self, kv_cache: torch.Tensor) -> None:
+        super().bind_kv_cache(kv_cache)
+        if self._b12x_prefill_api is not None:
+            self._b12x_prefill_plan = self._make_b12x_kda_prefill_plan(
+                max_state_slots=int(self.kv_cache[1].shape[0])
+            )
+
+    def _initialize_b12x_kda_prefill(self, vllm_config: VllmConfig) -> None:
+        """Keep only metadata tensors resident; scratch comes from the manager."""
+        if self.kda_prefill_backend != "b12x":
+            return
+        api = get_b12x_kda_prefill()
+        if api is None:
+            raise RuntimeError(
+                "The b12x KDA prefill backend requires the hash-pinned overlay."
+            )
+        device = torch.device(current_platform.current_device())
+        scheduler_config = vllm_config.scheduler_config
+        self._b12x_prefill_api = api
+        self._b12x_prefill_max_tokens = int(
+            scheduler_config.max_num_batched_tokens
+        )
+        self._b12x_prefill_max_seqs = int(scheduler_config.max_num_seqs)
+        max_seqs = self._b12x_prefill_max_seqs
+        self.register_buffer(
+            "_b12x_prefill_num_seqs",
+            torch.zeros(1, dtype=torch.int32, device=device),
+            persistent=False,
+        )
+        self.register_buffer(
+            "_b12x_prefill_num_tokens",
+            torch.zeros(1, dtype=torch.int32, device=device),
+            persistent=False,
+        )
+        self.register_buffer(
+            "_b12x_prefill_initial_indices",
+            torch.zeros(max_seqs, dtype=torch.int32, device=device),
+            persistent=False,
+        )
+        self.register_buffer(
+            "_b12x_prefill_null_indices",
+            torch.full(
+                (max_seqs,),
+                NULL_BLOCK_ID,
+                dtype=torch.int32,
+                device=device,
+            ),
+            persistent=False,
+        )
+        self.register_buffer(
+            "_b12x_prefill_zero_offsets",
+            torch.zeros(max_seqs, dtype=torch.int32, device=device),
+            persistent=False,
+        )
+        self.b12x_warmup_provider = _B12xKdaPrefillWarmup()
+
+    def _make_b12x_kda_prefill_plan(self, max_state_slots: int):
+        api = self._b12x_prefill_api
+        if api is None:
+            raise RuntimeError("b12x KDA prefill was not initialized")
+        return api.plan(
+            api.Caps(
+                device=current_platform.current_device(),
+                max_tokens=self._b12x_prefill_max_tokens,
+                max_seqs=self._b12x_prefill_max_seqs,
+                max_state_slots=max_state_slots,
+                heads=self.local_num_heads,
+                head_dim=self.head_dim,
+                model_dtype=self.model_config.dtype,
+                state_dtype=self.get_state_dtype()[1],
+                qk_l2norm=True,
+                # Preserve A59's state-pool geometry. Ironclad checkpoint
+                # export belongs to the later connector port, not this A/B.
+                checkpoint_export=False,
+                null_state_index=NULL_BLOCK_ID,
+                metadata_validation="trusted",
+            )
+        )
+
+    def _run_b12x_kda_prefill(
+        self,
+        *,
+        q: torch.Tensor,
+        k: torch.Tensor,
+        v: torch.Tensor,
+        raw_g: torch.Tensor,
+        raw_beta: torch.Tensor,
+        cu_seqlens: torch.Tensor,
+        state_indices: torch.Tensor,
+        has_initial_state: torch.Tensor,
+        recurrent_state: torch.Tensor,
+        scratch: torch.Tensor | list[torch.Tensor],
+        output: torch.Tensor,
+    ) -> None:
+        """Run B12X directly against the existing recurrent-state pool."""
+        api = self._b12x_prefill_api
+        plan = self._b12x_prefill_plan
+        if api is None or plan is None:
+            raise RuntimeError(
+                "b12x KDA prefill KV cache was not bound before inference"
+            )
+        num_tokens = int(q.shape[0])
+        num_requests = int(state_indices.shape[0])
+        if (
+            num_tokens > self._b12x_prefill_max_tokens
+            or num_requests > self._b12x_prefill_max_seqs
+        ):
+            raise ValueError(
+                "b12x KDA prefill capacity exceeded: "
+                f"tokens={num_tokens}/{self._b12x_prefill_max_tokens}, "
+                f"requests={num_requests}/{self._b12x_prefill_max_seqs}"
+            )
+
+        initial_indices = self._b12x_prefill_initial_indices[:num_requests]
+        initial_indices.copy_(state_indices)
+        initial_indices.masked_fill_(
+            ~has_initial_state[:num_requests], NULL_BLOCK_ID
+        )
+        null_indices = self._b12x_prefill_null_indices[:num_requests]
+        zero_offsets = self._b12x_prefill_zero_offsets[:num_requests]
+        self._b12x_prefill_num_seqs.fill_(num_requests)
+        self._b12x_prefill_num_tokens.fill_(num_tokens)
+
+        binding = api.bind(
+            plan,
+            scratch=scratch,
+            q=q,
+            k=k,
+            v=v,
+            raw_g=raw_g,
+            raw_beta=raw_beta,
+            A_log=self.A_log.view(-1),
+            dt_bias=self.dt_bias.view(-1, self.head_dim),
+            recurrent_state=recurrent_state,
+            cu_seqlens=cu_seqlens[: num_requests + 1],
+            initial_state_indices=initial_indices,
+            final_state_indices=state_indices,
+            checkpoint_state_indices=null_indices,
+            checkpoint_offsets=zero_offsets,
+            num_seqs=self._b12x_prefill_num_seqs,
+            num_tokens=self._b12x_prefill_num_tokens,
+            output=output,
+        )
+        api.run(
+            binding,
+            lower_bound=self.kda_lower_bound,
+            max_live_tokens=num_tokens,
+            max_live_seqs=num_requests,
+        )
+
     def forward(
         self,
         hidden_states: torch.Tensor,
@@ -637,10 +906,59 @@
             assert q_ns is not None
             assert non_spec_state_indices_tensor is not None
             assert has_initial_state is not None
-            initial_state = gather_initial_states(
-                recurrent_state, non_spec_state_indices_tensor, has_initial_state
+            use_b12x_prefill = self.kda_prefill_backend == "b12x"
+            initial_state = (
+                None
+                if use_b12x_prefill
+                else gather_initial_states(
+                    recurrent_state,
+                    non_spec_state_indices_tensor,
+                    has_initial_state,
+                )
             )
-            if self.kda_prefill_backend == "flashkda":
+            if use_b12x_prefill:
+                assert lower_bound is not None
+                assert non_spec_query_start_loc is not None
+                plan = self._b12x_prefill_plan
+                if plan is None:
+                    raise RuntimeError(
+                        "b12x KDA prefill KV cache was not bound before inference"
+                    )
+                num_prefill_tokens = int(q_ns.shape[0])
+                workspaces = current_workspace_manager().get_simultaneous(
+                    *((spec.shape, spec.dtype) for spec in plan.scratch_specs()),
+                    (
+                        (
+                            self._b12x_prefill_max_tokens,
+                            self.local_num_heads,
+                            self.head_dim,
+                        ),
+                        self.model_config.dtype,
+                    ),
+                )
+                *scratch_buffers, b12x_out = workspaces
+                b12x_scratch = (
+                    scratch_buffers[0]
+                    if len(scratch_buffers) == 1
+                    else scratch_buffers
+                )
+                b12x_out = b12x_out[:num_prefill_tokens]
+                self._run_b12x_kda_prefill(
+                    q=_rearr(q_ns)[0],
+                    k=_rearr(k_ns)[0],
+                    v=_rearr(v_ns)[0],
+                    raw_g=g1_ns[0],
+                    raw_beta=beta_ns[0],
+                    cu_seqlens=non_spec_query_start_loc,
+                    state_indices=non_spec_state_indices_tensor,
+                    has_initial_state=has_initial_state,
+                    recurrent_state=recurrent_state,
+                    scratch=b12x_scratch,
+                    output=b12x_out,
+                )
+                core_attn_out_non_spec = b12x_out.unsqueeze(0)
+            elif self.kda_prefill_backend == "flashkda":
+                assert initial_state is not None
                 assert lower_bound is not None
                 assert self._flashkda_buffer_specs is not None
                 workspace_out, final_state, workspace = (
@@ -670,6 +988,7 @@
                     workspace=workspace,
                 )
             else:
+                assert initial_state is not None
                 (
                     core_attn_out_non_spec,
                     last_recurrent_state,
@@ -690,12 +1009,14 @@
                     safe_gate=safe_gate,
                     lower_bound=lower_bound,
                 )
-            # Init cache
-            scatter_states(
-                recurrent_state,
-                last_recurrent_state,
-                non_spec_state_indices_tensor,
-            )
+            # B12X writes the existing pool directly; legacy paths return a
+            # dense final state that still needs the established scatter.
+            if not use_b12x_prefill:
+                scatter_states(
+                    recurrent_state,
+                    last_recurrent_state,
+                    non_spec_state_indices_tensor,
+                )
         elif attn_metadata_narrowed.num_decodes > 0:
             assert non_spec_query_start_loc is not None
             assert non_spec_state_indices_tensor is not None
